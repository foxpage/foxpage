export enum FileType {
  component = 'component',
  page = 'page',
  template = 'template',
  block = 'block',
  condition = 'condition',
  variable = 'variable',
  function = 'function',
  package = 'package',
}

export enum GlobalType {
  project = 'project',
  folder = 'folder',
  file = 'file',
  content = 'content',
}

export enum GlobalTypeColor {
  project = '#FF9900',
  folder = '#FF9900',
  page = '#87D068',
  template = '#2DB7F5',
  block = '#108EE9',
  content = 'default',
}

export const WIDTH_DEFAULT = 1136;
